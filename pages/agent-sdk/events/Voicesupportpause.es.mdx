# Voicesupportpause

Este evento es retorno del método voiceSupportPause con el resultado de la solicitud.

## Como escuchar

```js copy
import { olosOn } from "olosagentsdk";

olosOn("VoiceSupportPause", callback);
```
