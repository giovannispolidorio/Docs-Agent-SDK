# OlosIntegrationWs

Text

## listAvailableOnlineAgentsByCompany

```ts copy
import { OlosIntegrationWs } from "olosagentsdk";

const integrationWs = new OlosIntegrationWs();
integrationWs.listAvailableOnlineAgentsByCompany(agentId: any, callback?: (response: any) => void);
```

## listCampaignsToConsulting

```ts copy
import { OlosIntegrationWs } from "olosagentsdk";

const integrationWs = new OlosIntegrationWs();
integrationWs.listCampaignsToConsulting(campaignId: any, callback?: (response: any) => void);
```
